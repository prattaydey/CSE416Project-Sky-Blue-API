# Local Lahman Data

Put the SABR/Lahman comma-delimited zip here for local sync testing.

Example `.env`:

```env
LAHMAN_ZIP_PATH=data/lahman/lahman-csv.zip.zip
```

The zip itself is git-ignored to avoid committing large generated data files.
